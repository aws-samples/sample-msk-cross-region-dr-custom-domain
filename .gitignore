*.pyc
__pycache__/
.pytest_cache/
.venv/
cdk.out/
*.swp
.DS_Store
cdk.context.json

# IDE
.idea/
.vscode/

# Build/packaging
*.egg-info/
Recording/

# Threat model exports (attached to security review ticket separately)
.threatmodel/

# Bootstrap TLS certificate material (generated by scripts/gen_bootstrap_cert.sh).
# The private key MUST NEVER be committed. The public CA bundle is regenerated
# on demand and imported to ACM, so it is not tracked either.
certs/
scripts/bootstrap-ca.pem
